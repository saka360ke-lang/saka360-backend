<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Your Saka360 subscription invoice</title>
  <style>
    body{font-family:Arial,Helvetica,sans-serif;background:#f6f8fb;margin:0;padding:24px;color:#111}
    .card{max-width:640px;margin:0 auto;background:#fff;border-radius:12px;padding:24px;border:1px solid #e8edf3}
    .brand{font-weight:700;font-size:18px;color:#0d5bd7}
    .row{display:flex;gap:16px;flex-wrap:wrap}
    .col{flex:1 1 220px}
    .pill{display:inline-block;background:#eef4ff;color:#0d5bd7;padding:4px 8px;border-radius:999px;font-size:12px}
    .hr{height:1px;background:#eef2f7;border:0;margin:20px 0}
    .tot{font-weight:700}
    .btn{display:inline-block;background:#0d5bd7;color:#fff;text-decoration:none;padding:10px 14px;border-radius:8px;margin-top:8px}
    .meta{color:#667085;font-size:13px;margin-top:12px}
    table{width:100%;border-collapse:collapse}
    td{padding:8px 0;border-bottom:1px solid #f1f4f8}
  </style>
</head>
<body>
  <div class="card">
    <div class="brand">Saka360</div>
    <h2>Subscription Invoice</h2>
    <p>Hi {{user_name}}, here is your invoice for <span class="pill">{{plan_name}}</span>.</p>

    <div class="row">
      <div class="col">
        <p><strong>Invoice #</strong><br>{{invoice_number}}</p>
      </div>
      <div class="col">
        <p><strong>Invoice date</strong><br>{{date}}</p>
      </div>
      <div class="col">
        <p><strong>Billing period</strong><br>{{period_start}} → {{period_end}}</p>
      </div>
    </div>

    <div class="hr"></div>

    <table>
      <tr>
        <td>{{plan_name}} (annual)</td>
        <td style="text-align:right">{{currency}} {{amount}}</td>
      </tr>
      {{#if discount_label}}
      <tr>
        <td>{{discount_label}}</td>
        <td style="text-align:right">- {{currency}} {{discount_amount}}</td>
      </tr>
      {{/if}}
      <tr>
        <td class="tot">Total due</td>
        <td class="tot" style="text-align:right">{{currency}} {{total_due}}</td>
      </tr>
    </table>

    {{#if payment_link}}
      <a class="btn" href="{{payment_link}}" target="_blank">Pay now</a>
    {{/if}}

    <p class="meta">If you have already paid, you can ignore this message. For questions, reply to this email.</p>
  </div>
</body>
</html>
